{
  "schemaVersion": 1,
  "id": "ryuhzk.customer-reviews",
  "name": "Customer Reviews",
  "version": "0.1.0",
  "author": "ryuhzk",
  "license": "MIT",
  "description": "Read and reply to App Store customer reviews from the Omarchy bar.",
  "kinds": ["service", "bar-widget"],
  "keepLoaded": true,
  "entryPoints": {
    "service": "Service.qml",
    "barWidget": "BarWidget.qml"
  },
  "barWidget": {
    "displayName": "Customer Reviews",
    "description": "Inbox for App Store customer reviews, with replies and a watched-app badge.",
    "category": "Development",
    "allowMultiple": false,
    "defaultSection": "center",
    "defaults": {
      "panelWidth": 720,
      "pollIntervalSec": 10800,
      "unrepliedOnly": true
    },
    "schema": [
      {
        "key": "panelWidth",
        "type": "integer",
        "label": "Panel width",
        "min": 420,
        "max": 1100,
        "step": 10,
        "defaultValue": 720,
        "description": "Width of the popup panel in layout units."
      },
      {
        "key": "pollIntervalSec",
        "type": "integer",
        "label": "Background poll interval (seconds)",
        "min": 3600,
        "max": 43200,
        "step": 3600,
        "defaultValue": 10800,
        "description": "How often the service refreshes the unread badge and notifications. Opening the panel always loads once."
      },
      {
        "key": "unrepliedOnly",
        "type": "boolean",
        "label": "Show unreplied reviews first",
        "defaultValue": true,
        "description": "Default inbox filter after opening the panel."
      }
    ]
  }
}
